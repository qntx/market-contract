name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: qntx/workflows/.github/workflows/ci-foundry.yml@v2
    permissions:
      contents: read
    with:
      submodules: true
